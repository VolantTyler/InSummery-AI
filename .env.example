# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Google Calendar OAuth credentials
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# OpenTelemetry GenAI Semantic Conventions (v1.37.0+)
OTEL_SEMCONV_STABILITY_OPT_IN=gen_ai_latest_experimental

# Enable capturing message content (required for the hook to capture prompts/responses)
OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=true

# Enable the upload hook to offload large payloads to external storage
OTEL_INSTRUMENTATION_GENAI_COMPLETION_HOOK=upload

# Upload format: 'jsonl' or 'json'
OTEL_INSTRUMENTATION_GENAI_UPLOAD_FORMAT=jsonl

# Upload destination: GCS bucket for production, or a local directory for testing
# Production example: gs://your-telemetry-bucket/gen_ai
# Local development example: ./telemetry_uploads
OTEL_INSTRUMENTATION_GENAI_UPLOAD_BASE_PATH=./telemetry_uploads

# Local CLI: print OpenTelemetry spans to stderr (default: off, keeps demo output clean)
# INSUMMERY_OTEL_CONSOLE=true

# Optional Weights & Biases Weave observability.
# Keep WANDB_API_KEY out of source control; set it in your local .env or backend secret manager.
# WEAVE_PROJECT must be entity/project (e.g. your-team/insummery-ai), not just the project name.
WANDB_API_KEY=
WEAVE_PROJECT=your-wandb-entity/insummery-ai
WEAVE_DISABLED=true

# Optional: run Presidio over masked interpreter fields (email/phone/SSN/CC only).
# WEAVE_PRESIDIO_GUARDRAIL=true

# Optional: append sanitized HITL clarifications to a local eval-growth dataset.
# INSUMMERY_HITL_DATASET_APPEND=true
# INSUMMERY_HITL_DATASET_PATH=data/hitl_feedback_cases.json
